# Practico 5

Para ejecutar desde la misma computadora:

* Desde /build ejecutar:
    * ```bash
        start rmiregistry 3001
        ```
    * ```bash
        java Server
        ```
    * ```bash
        java Client
        ```
